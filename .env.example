# Environment Configuration for Local Development
# Copy this file to .env and update the values

NODE_ENV=development

# JWT Secret Key (change this in production)
JWT_SECRET=change-this-secret-to-something-secure

# MongoDB Connection String
# For local development, use MongoDB Atlas or local MongoDB
MONGODB_URI=mongodb://localhost:27017/tdtu-locket

# Server Port (for localhost)
PORT=3000

# CORS Origins (comma-separated)
CORS_ORIGINS=https://fe-locket-tdtu.vercel.app,http://localhost:5173,https://locket-tdtu.wangtech.top

# Vercel Environment Variables
# These will be automatically set in Vercel deployment
# You can also set them manually in Vercel dashboard:
# - JWT_SECRET
# - MONGODB_URI
